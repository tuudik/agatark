# Environment variables for Agatark MQTT Bridge

# Agatark System Configuration
# Host supports auto-detection:
#   - Domain names → HTTPS (e.g., my-agatark-system.com)
#   - IP addresses → HTTP (e.g., 192.168.1.100)  
#   - localhost → HTTP (e.g., localhost:8080)
#   - Explicit protocol → Use as provided (e.g., http://192.168.1.100 or https://secure.domain.com)
AGATARK_HOST=your-agatark-host.com
AGATARK_EMAIL=your.email@example.com
AGATARK_PASSWORD=your_password_here
AGATARK_REMEMBER=on

# MQTT Broker Configuration (Bridge acts as broker)
MQTT_PORT=1883
MQTT_USERNAME=homeassistant
MQTT_PASSWORD=ha_password

# Bridge Configuration
# Event polling interval in milliseconds (0 = continuous polling, recommended)
POLL_INTERVAL=0
LOG_LEVEL=info
DISCOVERY_PREFIX=homeassistant

# Device Filtering (comma-separated device IDs, leave empty for all)
DEVICE_FILTER=

# Home Assistant Discovery
HA_DISCOVERY_ENABLED=true
HA_DEVICE_NAME=Agatark Smart Home